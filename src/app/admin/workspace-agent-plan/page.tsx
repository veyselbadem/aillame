import { WorkspaceAgentPlanPreview } from '../../../components/agent/WorkspaceAgentPlanPreview';

export const metadata = {
  title: 'Workspace Agent Plan Preview | Admin',
};

export default function WorkspaceAgentPlanPreviewPage() {
  return <WorkspaceAgentPlanPreview />;
}